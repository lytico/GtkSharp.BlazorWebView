@page "/"

@using BlazorWebKit.Test.Components
@using System.Diagnostics

<Counter />

<p>@nameof(StartupTime) @StartupTime</p>

@code {
    string StartupTime
    {
        get
        {
            return (Process.GetCurrentProcess().StartTime - DateTime.Now).ToString() + "x";
        }
    }
}